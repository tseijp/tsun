import {atom} from 'jotai'

export const rowAtom = atom(0)
export const colAtom = atom(0)
export const pagesAtom = atom(["readed", "reading", "to read"])
export const entryAtom = atom(["World", "Business", "Living", "Learn", "Tech"])
